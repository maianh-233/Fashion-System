import ProductImageSection from "../Product/ProductImageSection";

/** Variant image editor backed by the secured Cloudinary Product image API. */
export default function ProductVariantImageSection(props) {
  return <ProductImageSection {...props} />;
}
